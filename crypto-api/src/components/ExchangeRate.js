const ExchangeRate = ({exchangeRate}) => {
    return (
      <div className="exchange-rate">
        <h3>Exchange Rate</h3>
        <h1>{exchangeRate}</h1>
      </div>
    );
  }
  
  export default ExchangeRate;